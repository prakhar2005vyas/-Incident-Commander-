# Sandbox Scripts

Scripts executed inside the Daytona sandbox by the Incident Commander agent during investigation.

- `bisect.py`: Bisection tool that inspects metric snapshots and deploy commits to isolate the exact commit causing an incident.
