# Metrics MCP Server

Model Context Protocol (MCP) server providing read-only access to victim-app telemetry.

### Tools:
- `get_error_rate(service, time_range)`: Returns error rate percentage and timestamp.
- `get_latency(service, time_range)`: Returns p95 latency in milliseconds.
